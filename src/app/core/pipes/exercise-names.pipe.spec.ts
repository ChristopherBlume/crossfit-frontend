import { ExerciseNamesPipe } from './exercise-names.pipe';

describe('ExerciseNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new ExerciseNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
